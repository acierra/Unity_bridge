# Vp8DecoderBridge

Minimal Linux native bridge around host `libvpx` for decoding VP8 frames received by the Unity WebRTC smoke receiver.

Build:

```bash
make -C Plugin/Vp8DecoderBridge/Project/Linux
```

Output:

```text
Plugin/Vp8DecoderBridge/Build/Linux/libVp8DecoderBridge.so
```
